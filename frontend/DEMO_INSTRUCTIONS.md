# Инструкция по запуску и демонстрации фронтенда

## Лабораторная работа №5 - React SPA приложение

### Цель работы
Разработка базового SPA на React для работы с каталогом вазоактивных препаратов.

---

## 📋 Что реализовано

### ✅ Три страницы приложения:
1. **Главная страница (/)** - Статическое описание системы с информацией о функциях
2. **Каталог препаратов (/drugs)** - Список всех препаратов с фильтрацией
3. **Детальная страница препарата (/drugs/:id)** - Полная информация о выбранном препарате

### ✅ Компоненты React-Bootstrap:
- Navbar - Навигационная панель
- Container, Row, Col - Сетка Bootstrap
- Card - Карточки препаратов
- Form, Button - Элементы фильтров
- Spinner - Индикатор загрузки

### ✅ Навигация:
- **Navbar** - Главное меню приложения (Главная / Каталог препаратов)
- **Breadcrumbs** - Навигационная цепочка, показывающая путь от главной страницы к текущей

### ✅ Фильтрация с бэкенда:
- По названию препарата
- По минимальной концентрации (мг/мл)
- По максимальной концентрации (мг/мл)
- Кнопка "Сбросить" для очистки фильтров

### ✅ Mock данные:
- При отсутствии доступа к бэкенду автоматически используются mock данные
- 5 препаратов с полной информацией
- Фильтрация работает и на mock данных

### ✅ Изображения:
- Поддержка изображений из MinIO (порт 9000)
- Placeholder изображение для препаратов без фото
- Обработка ошибок загрузки изображений

### ✅ Proxy для CORS:
- Все запросы к `/api/*` проксируются на `http://127.0.0.1:8000`
- Настроено в `vite.config.ts`

---

## 🚀 Порядок запуска

### 1. Установка зависимостей (если еще не установлены)

```bash
cd /root/RIP/frontend
npm install
```

### 2. Запуск фронтенда (БЕЗ бэкенда - с mock данными)

```bash
cd /root/RIP/frontend
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:3000**

### 3. Запуск Django бэкенда (для работы с реальными данными)

В отдельном терминале:

```bash
cd /root/RIP
python manage.py runserver
```

Django будет работать на **http://127.0.0.1:8000**

### 4. Запуск MinIO (для изображений)

MinIO уже должен быть запущен в контейнере на **порту 9000**.

---

## 📊 Порядок показа преподавателю

### Шаг 1: Показать работу с Mock данными (БЕЗ бэкенда)

1. **Убедиться, что Django НЕ запущен**
2. Запустить фронтенд: `npm run dev`
3. Открыть http://localhost:3000

**Что показать:**
- ✅ Главная страница с описанием системы
- ✅ Переход в каталог через кнопку "Перейти к каталогу препаратов"
- ✅ Страница каталога с 5 препаратами из mock данных
- ✅ Работа фильтров на mock данных:
  - Фильтр по названию (например, "Адреналин")
  - Фильтр по концентрации (мин: 1, макс: 10)
  - Кнопка "Сбросить"
- ✅ Breadcrumbs: Главная / Каталог препаратов
- ✅ Navbar с переключением между страницами
- ✅ Клик на карточку препарата → переход на детальную страницу
- ✅ Breadcrumbs на детальной: Главная / Каталог препаратов / [Название]
- ✅ Placeholder изображения (так как MinIO может быть недоступен)

**В DevTools → Network:**
- Показать попытки обращения к `/api/drugs/` (будут ошибки 502/503)
- Показать, что приложение не падает, а использует mock данные

### Шаг 2: Показать работу с бэкендом

1. **Запустить Django:** `python manage.py runserver`
2. Обновить страницу в браузере

**Что показать:**
- ✅ Препараты теперь загружаются из Django API
- ✅ В DevTools → Network показать успешные запросы:
  - `GET /api/drugs/` → 200 OK
  - `GET /api/drugs/1/` → 200 OK
- ✅ Фильтрация работает через бэкенд:
  - В Network видны запросы с query параметрами:
    - `/api/drugs/?name=адреналин`
    - `/api/drugs/?concentration_min=1&concentration_max=10`
- ✅ Изображения загружаются из MinIO (если настроены)
  - URL изображений: `http://localhost:9000/images/...`

### Шаг 3: Внести изменения в БД и показать во фронтенде

1. **Открыть Django Admin:** http://127.0.0.1:8000/admin
2. Добавить новый препарат или изменить существующий
3. **Обновить страницу каталога** в браузере
4. Показать, что изменения отобразились

### Шаг 4: Объяснить код

#### 📁 Структура проекта:
```
frontend/
├── src/
│   ├── components/         # Переиспользуемые компоненты
│   │   ├── AppNavbar.tsx   # Навигационная панель
│   │   ├── Breadcrumbs.tsx # Навигационная цепочка
│   │   └── DrugCard.tsx    # Карточка препарата
│   ├── pages/              # Страницы
│   │   ├── HomePage.tsx
│   │   ├── DrugsPage.tsx   # Список с фильтрами
│   │   └── DrugDetailPage.tsx
│   ├── mock/
│   │   └── DrugMock.ts     # Mock данные
│   ├── DrugTypes.ts        # TypeScript интерфейсы
│   ├── drugsApi.ts         # Функции для работы с API
│   └── App.tsx             # Роутинг
```

#### 🎯 Ключевые моменты в коде:

**1. drugsApi.ts - Fetch запросы:**
```typescript
export async function listDrugs(params?: { 
  name?: string; 
  concentration_min?: number;
  concentration_max?: number;
}): Promise<Drug[]> {
  try {
    let path = "/api/drugs/";
    // Формирование query параметров
    if (params) {
      const query = new URLSearchParams();
      if (params.name) query.append("name", params.name);
      // ...
    }
    const res = await fetch(path, { headers: { Accept: "application/json" } });
    return await res.json();
  } catch (err) {
    return []; // Fallback к пустому массиву
  }
}
```

**2. DrugsPage.tsx - Работа с фильтрами:**

**Props (состояния):**
```typescript
const [drugs, setDrugs] = useState<Drug[]>([]);  // Список препаратов
const [searchName, setSearchName] = useState(""); // Фильтр по имени
const [concentrationMin, setConcentrationMin] = useState("");
const [concentrationMax, setConcentrationMax] = useState("");
const [loading, setLoading] = useState(false);    // Индикатор загрузки
```

**Хуки:**
```typescript
// useEffect - загрузка данных при монтировании компонента
useEffect(() => {
  loadDrugs();
}, []);

// Функция загрузки с fallback к mock
const loadDrugs = async () => {
  setLoading(true);
  try {
    const data = await listDrugs();
    if (data.length > 0) {
      setDrugs(data);  // Используем данные с бэкенда
    } else {
      setDrugs(mockDrugs);  // Fallback к mock
    }
  } catch {
    setDrugs(mockDrugs);
  }
  setLoading(false);
};
```

**Обработчик поиска:**
```typescript
const handleSearch = async () => {
  setLoading(true);
  try {
    const params: any = {};
    if (searchName) params.name = searchName;
    if (concentrationMin) params.concentration_min = parseFloat(concentrationMin);
    if (concentrationMax) params.concentration_max = parseFloat(concentrationMax);

    const filtered = await listDrugs(params);
    
    if (filtered.length > 0) {
      setDrugs(filtered);  // Результаты с бэкенда
    } else {
      // Фильтрация mock данных на клиенте
      let result = mockDrugs;
      if (searchName) {
        result = result.filter(d => 
          d.name.toLowerCase().includes(searchName.toLowerCase())
        );
      }
      // ...
      setDrugs(result);
    }
  } catch {
    // Обработка ошибок
  }
  setLoading(false);
};
```

**3. DrugCard.tsx - Props компонента:**
```typescript
interface DrugCardProps {
  drug: Drug;  // Проп - объект препарата
}

export default function DrugCard({ drug }: DrugCardProps) {
  // Компонент получает данные через props
  return (
    <Card>
      <Card.Img 
        src={drug.image_url || DEFAULT_IMAGE}  // Fallback изображение
        onError={(e) => {
          (e.target as HTMLImageElement).src = DEFAULT_IMAGE;
        }}
      />
      <Card.Title>{drug.name}</Card.Title>
      {/* ... */}
    </Card>
  );
}
```

**4. vite.config.ts - Proxy для CORS:**
```typescript
export default defineConfig({
  server: { 
    port: 3000,
    proxy: {
      "/api": {
        target: "http://127.0.0.1:8000",  // Django backend
        changeOrigin: true,
        secure: false,
      },
    },
  },
  plugins: [react()],
})
```

---

## 🔍 Ключевые концепции React

### Хуки:
- **useState** - управление состоянием компонента
- **useEffect** - жизненный цикл (загрузка данных при монтировании)
- **useParams** - получение параметров из URL (id препарата)

### Props:
- Передача данных от родительского компонента к дочернему
- Типизация через TypeScript интерфейсы

### Fetch запросы:
- Асинхронные функции с try/catch
- Fallback к mock данным при ошибках

### React Router:
- Навигация без перезагрузки страницы
- Динамические маршруты (`/drugs/:id`)

---

## ✅ Чек-лист для показа

- [ ] Три страницы работают
- [ ] Mock данные загружаются без бэкенда
- [ ] Фильтрация работает на фронте (mock) и бэкенде
- [ ] Breadcrumbs отображают правильный путь
- [ ] Navbar переключает между страницами
- [ ] Placeholder изображение работает
- [ ] В Network видны запросы к `/api/drugs/`
- [ ] Proxy через Vite работает (нет CORS ошибок)
- [ ] Изменения в БД отображаются во фронтенде
- [ ] Объяснены хуки, props, fetch

---

## 📝 Полезные команды

```bash
# Запуск фронтенда
cd /root/RIP/frontend
npm run dev

# Запуск Django
cd /root/RIP
python manage.py runserver

# Сборка для production
cd /root/RIP/frontend
npm run build

# Остановка dev сервера
Ctrl + C
```

---

## 🎓 Готово к защите!

Все требования лабораторной работы выполнены:
- ✅ Три страницы (главная, каталог, детальная)
- ✅ React-Bootstrap компоненты
- ✅ Фильтрация с бэкенда
- ✅ Mock данные при отсутствии API
- ✅ Navbar и Breadcrumbs
- ✅ Placeholder изображения
- ✅ Proxy для CORS
- ✅ TypeScript типизация
- ✅ Хуки и Props объяснены
